# WeGov front-end environment (Payload CMS migration)
#
# The site now reads content from the Sarapis Payload CMS (multi-brand), not Strapi.
NEXT_PUBLIC_PAYLOAD_URL=https://next.sarapis.org
NEXT_PUBLIC_SITE_KEY=wegovnyc

# Legacy — still read as an origin fallback during cutover; safe to remove once
# NEXT_PUBLIC_PAYLOAD_URL is set everywhere (local + Vercel).
# NEXT_PUBLIC_STRAPI_URL=https://strapi.wegov.nyc

# Preview/draft secret (unchanged)
# PREVIEW_SECRET=...
